[Unit]
Description="Gollum wiki"

[Service]
User={{ wiki_user.name }}
Group={{ wiki_user.name }}
Type=Simple
ExecStart=/usr/local/bin/gollum {{ wiki_dir_path }} --ref main --allow-uploads page --critic-markup --h1-title --mathjax --emoji --no-display-metadata

[Install]
WantedBy=multi-user.target
